# Script2 .- Del script anterior crear un usuario con directorio personal, contraseña predeterminada, shell /bin/zsh y el comentario "act2" con solo el nombre.

useradd -m -s /bin/zsh $1 -p $1 -c "act2";

  # d = Directorio
  # s = Shell
  # p = password
  # c = Comentario


# Comandos por consola 
./Script2 invitado 
cat /etc/passwd
